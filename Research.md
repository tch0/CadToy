<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
**Table of Contents**  *generated with [DocToc](https://github.com/thlorenz/doctoc)*

- [CAD程序研究](#cad%E7%A8%8B%E5%BA%8F%E7%A0%94%E7%A9%B6)
  - [功能研究与实现](#%E5%8A%9F%E8%83%BD%E7%A0%94%E7%A9%B6%E4%B8%8E%E5%AE%9E%E7%8E%B0)
  - [BUG](#bug)
  - [非模态命令交互研究](#%E9%9D%9E%E6%A8%A1%E6%80%81%E5%91%BD%E4%BB%A4%E4%BA%A4%E4%BA%92%E7%A0%94%E7%A9%B6)
  - [窗口焦点相关问题](#%E7%AA%97%E5%8F%A3%E7%84%A6%E7%82%B9%E7%9B%B8%E5%85%B3%E9%97%AE%E9%A2%98)
  - [快捷健](#%E5%BF%AB%E6%8D%B7%E5%81%A5)
  - [选择功能研究](#%E9%80%89%E6%8B%A9%E5%8A%9F%E8%83%BD%E7%A0%94%E7%A9%B6)
  - [命令注册、命令别名、命令补全](#%E5%91%BD%E4%BB%A4%E6%B3%A8%E5%86%8C%E5%91%BD%E4%BB%A4%E5%88%AB%E5%90%8D%E5%91%BD%E4%BB%A4%E8%A1%A5%E5%85%A8)
  - [空间索引实现研究](#%E7%A9%BA%E9%97%B4%E7%B4%A2%E5%BC%95%E5%AE%9E%E7%8E%B0%E7%A0%94%E7%A9%B6)
  - [选择集功能研究](#%E9%80%89%E6%8B%A9%E9%9B%86%E5%8A%9F%E8%83%BD%E7%A0%94%E7%A9%B6)
  - [属性栏](#%E5%B1%9E%E6%80%A7%E6%A0%8F)
  - [捕捉与追踪功能研究](#%E6%8D%95%E6%8D%89%E4%B8%8E%E8%BF%BD%E8%B8%AA%E5%8A%9F%E8%83%BD%E7%A0%94%E7%A9%B6)
  - [脚本交互研究](#%E8%84%9A%E6%9C%AC%E4%BA%A4%E4%BA%92%E7%A0%94%E7%A9%B6)
  - [动态输入研究](#%E5%8A%A8%E6%80%81%E8%BE%93%E5%85%A5%E7%A0%94%E7%A9%B6)
  - [功能实现顺序](#%E5%8A%9F%E8%83%BD%E5%AE%9E%E7%8E%B0%E9%A1%BA%E5%BA%8F)
    - [第一阶段：核心架构、数据逻辑分离](#%E7%AC%AC%E4%B8%80%E9%98%B6%E6%AE%B5%E6%A0%B8%E5%BF%83%E6%9E%B6%E6%9E%84%E6%95%B0%E6%8D%AE%E9%80%BB%E8%BE%91%E5%88%86%E7%A6%BB)
    - [第二阶段：渲染管线、画布集成](#%E7%AC%AC%E4%BA%8C%E9%98%B6%E6%AE%B5%E6%B8%B2%E6%9F%93%E7%AE%A1%E7%BA%BF%E7%94%BB%E5%B8%83%E9%9B%86%E6%88%90)
    - [第三阶段：交互框架、状态机](#%E7%AC%AC%E4%B8%89%E9%98%B6%E6%AE%B5%E4%BA%A4%E4%BA%92%E6%A1%86%E6%9E%B6%E7%8A%B6%E6%80%81%E6%9C%BA)
    - [第四阶段：依次完善功能](#%E7%AC%AC%E5%9B%9B%E9%98%B6%E6%AE%B5%E4%BE%9D%E6%AC%A1%E5%AE%8C%E5%96%84%E5%8A%9F%E8%83%BD)
  - [透明命令研究](#%E9%80%8F%E6%98%8E%E5%91%BD%E4%BB%A4%E7%A0%94%E7%A9%B6)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

# CAD程序研究

## 功能研究与实现

已实现功能列表：
- 渲染循环
- 日志系统
- 各种界面元素：菜单栏、文件栏、命令栏、属性栏、状态栏
- 画布实现、栅格坐标轴光标绘制
- 各种坐标变换研究与封装：世界坐标系到屏幕、屏幕到世界、视口裁剪等
- 文件管理实现部分
- Options对话框
- 画布与命令栏上的键盘输入导入到命令栏输入框中提交执行
- 保存所有输入键盘信息，注册与识别快捷键
- 不同文档的渲染相关数据保存在在文档中
- 粗糙实现命令执行的基本流程
- 完善命令执行的基本框架
    - 输入上下文实现数值获取、字符串获取、点输入、分支选择功能
    - 清晰输入处理的职责：
        - InputHandler负责处理所有底层事件：按键、鼠标
        - 遇到字符/BackSpace则转发到Renderer输入框中，Renderer输入框仅处理字符编辑操作
        - Enter/Space/Esc时Renderer将整个字符串作为结果发送到InputContext中处理
            - 命令执行过程中的输入由InputContext处理后保存为自身状态，等待命令执行过程来获取
            - 非命令执行过程中则交由CommandManager解析为命令执行
    - 提示输出的职责划分与梳理
    - 一个时刻只能执行一条命令，可以手动多次Esc取消当前命令，也可以通过调用函数直接取消当前命令(通过管理输入上下文模拟多次Esc来实现)
    - [命令注册、命令别名、命令补全、命令历史](#%E5%91%BD%E4%BB%A4%E6%B3%A8%E5%86%8C%E5%91%BD%E4%BB%A4%E5%88%AB%E5%90%8D%E5%91%BD%E4%BB%A4%E8%A1%A5%E5%85%A8)
- 窗口焦点管理：
    - 画布上进行键盘输入时焦点自动切换到命令输入框进行输入交互
    - Enter/Space/Esc/Backspace自动将焦点切换到输入框
    - 实现鼠标移出非模态窗口就自动失去焦点的功能，以实现只要鼠标在画布上就能进行输入（而不是需要点击一下画布将焦点从某个窗口释放才能输入，这种操作会破坏CAD的交互逻辑，因为点击画布是点输入的一种交互）
- 光标形状定制：
    - 支持十字、选择框、选择框+十字、手掌四种光标形状
    - 实现光标右上角的标记：目前已实现向左向右框选、锁定标记、加选减选、正交标记
    - 移动、旋转、缩放、复制、裁剪/删除标记实现
    - 优先级：限制类 > 约束 > 即时编辑动作 > 选择模式，框选模式是鼠标瞬时状态，鼠标拖动时覆盖其他标记。
    - 优先级也具备特殊情况，比如erase命令在执行窗口或者交叉选择时，如果已经有了实体被高亮那么优先显示erase标记，实现相关命令时单独考虑处理即可
    - TODO: 将所有marker全部写进一个大的 VBO 里，通过 Offset 来切换显示？
- 选择交互：
    - 通过一个SelectionTask实现所有选择交互：单选、框选、栏选、多边形选择、套索选择
    - 细节见[选择功能研究](#%E9%80%89%E6%8B%A9%E5%8A%9F%E8%83%BD%E7%A0%94%E7%A9%B6)
    - 使用和命令同样的架构来实现，并且集成在InputContext内部
    - 测试梳理所有的分支，保证流程正确，提示正确输出与显示
    - 命令中调用只会从单选入口进入，测试确保流程正确
    - 为选择集的实现预留了逻辑与注释
    - TODO: 目前选择交互基本没有任何问题了，多边形绘制不太完美，只能绘制凸多边形，后续渲染引擎系统性升级时来改
- 点获取交互：
    - 点获取交互支持距离输入：
        - 为带基点的版本实现距离输入支持，最终得到的点是基点到当前预览点方向上距基点该距离的点。获取预览点-基点的方向向量，乘以距离加到基点上即可。
        - 在Autocad中不带基点的版本使用Lastpoint作为基点，这里则选择不为无基点版本做支持。
    - 点获取交互支持点坐标输入，支持二维点输入，当然也支持三维点输入，但会直接舍弃z坐标。
- 快捷键：
    - Ctrl+Z支持命令内识别，需要特殊处理，等效于输入字符u，并按下回车键。
    - 其他快捷键则直接取消当前命令后执行对应命令。
- 渲染器更新：
    - 使用现代OpenGL管线重构。
    - 栅格、坐标轴、光标、光标标记、选择相关图元由CanvasRenderer进行渲染，在屏幕坐标系下渲染。
    - 实体则由EntityRenderer在世界坐标系下渲染，渲染到离屏FBO后混合到默认FBO上，离屏FBO和默认FBO一样大，但是只渲染到视口区域内。
    - 渲染叠加顺序：
        - 背景层：栅格、坐标轴
        - 实体层：所有实体，包括临时实体、橡皮线、高亮等
        - 前景层：夹点、选择、光标、捕捉追踪相关图元
        - 背景层前景层由CanvasRenderer负责渲染，实体层EntityRenderer。
- 高分辨率支持、DPI缩放适配、多显示器配置：
    - UI尺寸跟随字体大小修改而变化。
    - ImGui支持动态修改字体大小，v1.92后会动态按需加载字体，不会有性能问题。
    - 计算UI布局时使用ImGui接口获取到窗口大小、Frame高度、各种间隔等值，就可以实现UI随字体大小缩放。
    - 而如果一定要使用硬编码高度、宽度那就需要乘以一个UI缩放比例（当前字号与默认字号的比值）就可以做到。
    - 支持字号：18-50。
    - 实现了多显示器及DPI的配置，保存到文件，启动时读取：
        - 没有配置时使用默认字号18 * DPI。
        - 对每一个显示器尺寸和DPI的组合，保存一个配置，互不影响。
        - 窗口移动到其他屏幕、修改了DPI缩放比例（通过监听DPI变化以及窗口移动做到）则切换配置。
- 字符串与路径编码问题


Todo：
- 命令执行框架中加入对透明命令的支持
- 实体实现，跑通文件流读取保存的流程，探索数据格式设计，实现保存、读取等命令，改造Line命令使其数据持久化。
- 实体级、命令级的undo redo研究与实现
- drag过程实现，以Line为例
- 跑通Line命令
- 图形引擎实现，将复杂实体拆分成简单的片元，打包渲染
- 多文档渲染到多个FBO
- Imgui切换到Docking分支，支持将文档拖出
- 参考line实现常见实体类型，常见创建命令实现：xline、pline、circle、arc、rect等
- 捕捉实现、使用空间四叉树/R树来实现
- 选择集研究与实现
- 研究几何库、封装几何操作
- 几何编辑框架与相关命令研究与实现
- 快捷键：
    - F1帮助
    - F2打开新文本窗口
    - 命令栏输入可能会使用的快捷键需要区分单独处理，比如Ctrl+V/Ctrl+A，在命令输入栏进行命令输入时是粘贴字符串，全选已输入的所有字符，在绘图区是粘贴图形、全选所有实体
- 命令历史使用InputMultiText实现，使命令历史可选中
- 修改主循环，拦截窗口关闭操作，替换为自己的窗口关闭逻辑，弹窗，继续渲染循环，
    - 方法1：回调函数glfwSetWindowShouldClose设置标记
    - 方法2：主循环中拦截窗口关闭消息，glfwWindowShouldClose(window, GLFW_FALSE)阻止窗口关闭，不调用拦截关闭动作的话，下一帧渲染循环就会结束
    - 用方法2，依次调用每个文档的关闭操作就好了，有一个不关闭就留下来
    - 待仔细研究
- UCS支持：待研究

细节问题或者功能TODO：
- 优先级特殊情况：框选模式下，erase命令框里面有实体会被选中时，优先显示erase标记，没有实体才显示框选标记
- 点获取交互：
    - 可以考虑通过接口设置是否支持三维点输入。
    - 二维CAD软件z坐标称为标高，作用有限，目前还不准备做支持。
    - 很多命令比如trim/extend等命令要求实体在同一个平面内，z坐标不为0实施不了。
    - 支持正交模式：按住shift，进入正交模式，只能选择平行与XY轴的点，此时预览点不再是鼠标点，需要进行一定计算。
    - 支持橡皮线显示：暂时不用支持，支持不同类型橡皮线，可控制显示或者不显示，提供接口用来设置。
- 命令中调用选择需要支持用户定义的关键字，用户给的关键字优先级高于waitForPoint的关键字优先级，如果有相同关键字优先返回给用户。
    - 选择任务内部需要单选分支需要特殊处理以支持。
    - 其他分支不需要支持，仅需要支持入口。
    - 可以通过隔离用户关键字与选择中的关键字来做。
    - 同时需要考虑仅支持单选而不能框选的模式（更一般地，定制允许的选择模式）。
    - 后续考虑实现更完善的选择功能的时候再来做。
- 检查研究程序最小化后的行为：此时可能不必要执行很多行为，但也要保证基本的正确性和数据有效性。
- Logger中日志文件读写目前并未启用，后续如果要启用，需要仔细检查相关逻辑，特别是路径编码问题。
- GLFuncs.cpp中也有文件读写操作，也需要处理路径编码问题，不过目前没有用到，后续整理时再来。
- EntityRenderer中关于Shader的逻辑可能还需要整理，Shader定义了一堆函数并没有用到。

其他待实现细节研究：
- 选择点时，按住Shift强制进入正交模式，会有marker标记
- 第三方R树（或者自己实现）实现空间索引（如Boost.Geometry 或 libspatialindex），`boost::geometry::index::rtree`
- 考虑使用更现代的ECS (Entity Component System，实体组件系统) 实现数据结构，而不是使用传统的继承，以实现超强的性能（百万量级图元），C++ECS库EnTT。
- 实体渲染：预选中、选中高亮
- 后续夹点实现：可以在InputContext中实现GripTask，在onUpdate中查询各个task是否激活，决定调用各个task的onUpdate
- 命令执行中的分支选择历史与点输入历史，待研究。
- 命令栏在输入命令时，也就是输入框不为空时，画布是锁定的，动态输入模式下光标都是锁定的，不会响应任何选择、点击操作。

## 字符串与路径编码问题

本地字符串：
- 所有项目字符串都保存在`std::string`中，使用UTF-8编码，所有`std::string`都默认为UTF-8编码。
- ImGui也仅支持UTF-8编码，直接`std::string::c_str`即可用于输出。
- 重点在于路径的处理上。

路径编码：
- `std::filesystem::path`原生支持本地编码和UTF-8，自动处理，不再需要任何封装。
- 但是`std::filesystem::path`的`string u8string`接口分别返回本地编码和UTF-8编码。
- 而主程序只使用`std::string`来存放UTF-8编码，所以在`std::filesystem::path`和本地项目中的字符串的边界需要进行合适转换：
    - 不能直接调用`string`接口返回本地编码，然后通过接口去做编码转换。这反而饶了远路。
    - 最简单的方式是直接调用`u8string`得到`std::u8string`，但项目中是不支持`char8_t`字符串的，因为不方便输出，而`char8_t`是可以安全转换为`char`的。
    - 在项目中引入任何`char8_t`字符串也容易造成混淆。
- 所以可以约定：
    - `std::filesystem::path`和本地UTF-8编码`std::string`之间的转换不应该使用原生的`std::filesystem::path`的API。
    - 所有路径操作应该统一转换为`path`之后再去进行，不应该自己拼接字符串，也不要拼接`path`和UTF-8`string`字符串。ASCII常量字符串则不受影响可以直接和`path`拼接。
    - 调用文件API时尽量使用C++标准库`fstream`，以`path`作为参数。
    - 项目中不使用任何`std::u8string`来存储字符串。
    - 定义转换接口`PathUtils::toPath/toString`来替代`std::filesystem::path(std::string)`构造和`std::filesystem::path::string()`接口作为字符串和路径之间的桥梁。
    - 项目中不应该在任何地方再使用`std::filesystem::path(std::string)`和`std::filesystem::path::string()`。
- 转换接口：
    - 几乎不会有性能损失，inline下`toPath`直接头尾指针构造不会有性能损失，`toString`则多构造了一次，损失非常小。
```C++
namespace PathUtils {
// UTF-8 string -> filesystem::path
inline std::filesystem::path toPath(const std::string& utf8String) {
    return std::filesystem::path(reinterpret_cast<const char8_t*>(utf8String.data()),
                                 reinterpret_cast<const char8_t*>(utf8String.data()) + utf8String.size());
}
// filesystem::path -> UTF-8 string
inline std::string toString(const std::filesystem::path& path) {
    const std::u8string u8 = path.u8string();
    return std::string(u8.begin(), u8.end());
}
} // namespace PathUtils
```

## BUG

已修复BUG：
- 命令补全问题：焦点不在命令输入框时，输入L，Tab补全为LINE，Esc，此时命令候选框不会消失。待调试，很奇怪的bug。
    - 特定操作顺序才会复现，很corner的case，统一在回调里判断输入为空时清空补全信息，确保一定不会出弹出不应该弹出的补全候选框。

发现的未修复BUG：
- 字体文件缺失好像会直接assert失败在imgui逻辑中，应该fallback到默认字体。
- 原点不在屏幕内时坐标轴不显示。

## 非模态命令交互研究

在命令的交互过程中，核心交互是拾取点、获取关键字、获取整数浮点数字符串这类非模态交互：
- 获取输入过程中，每一帧的绘图不会停止，命令却是停止运行的。
- 获取到输入后，绘图同样继续进行，命令继续执行。

实现方案1：
- 将命令实现为状态机，将命令分割为一系列由交互隔离的状态：
```C++
class LineCommand : public IncrementalCommand {
    int stage = 0;
    Vec2 p1, p2;

    void OnUpdate(InputContext& ictx) override {
        switch (stage) {
            case 0:
                ictx.SetPrompt("指定第一点:");
                if (ictx.GetPickedPoint(p1)) stage = 1;
                break;
            case 1:
                ictx.SetPrompt("指定下一点:");
                ictx.DrawRubberBand(p1); // 在渲染层画预览线
                if (ictx.GetPickedPoint(p2)) {
                    SubmitLine(p1, p2);
                    p1 = p2; // 循环
                }
                break;
        }
    }
};
```
- 在主循环中，每一帧都会调用命令的update，去检测是否获取到了输入并更新命令的状态。

实现方案2：
- 使用C++20的无栈协程来完成异步逻辑，将命令逻辑和获取输入的逻辑置于不同协程内。
```C++
Task Command_Line(CadContext& ctx) {
    // 挂起并等待输入，主循环每帧会 Check 状态
    Vec2 p1 = co_await ctx.GetPoint("指定第一点");
    
    while (true) {
        // 这里的 p2 在用户移动鼠标时会提供“预览点”
        Vec2 p2 = co_await ctx.GetPoint("指定下一点", p1); // 传入 p1 用于画橡皮筋预览线
        ctx.Database.AddLine(p1, p2);
        p1 = p2; // 连续画线逻辑
    }
}
```
- 问题：C++20有栈协程是侵入式的，具有传染性，命令本身、一系列交互函数以及调用这些的入口都需要是协程。

实现方案3：
- 使用有栈协程，比如Windows的Fiber、Boost.context等。
```C++
Vec2 GetPoint(const char* prompt) {
    g_CommandContext.SetPrompt(prompt);
    
    // 关键：挂起当前命令协程，回到主线程渲染
    g_CommandContext.YieldToMain(); 
    
    // 当主线程检测到输入并 Resume 后，代码从这里继续执行
    return g_CommandContext.GetLastPickedPoint();
}

void LineCommand::Execute() {
    Vec2 p1 = GetPoint("指定第一点:");
    while(true) {
        // 第二次调用 GetPoint 时，主线程会不断更新“预览线”
        Vec2 p2 = GetPoint("指定下一点:"); 
        AddLineToDatabase(p1, p2);
        p1 = p2;
    }
}
```
- 优点：非侵入式、和旧标准代码完美协作。

实现考虑：
- C++20无栈协程侵入性和传染性太强，对架构影响非常大，这里暂时不考虑。
- 先实现为状态机，后续如果合适可以切换为有栈协程。

## 窗口焦点相关问题

## 快捷健

## 选择功能研究

分类：
- 点选：点击一个实体即将其加入选择集，只要实体和拾取框有重叠即可选中
- 框选：通过两个点确定矩形范围进行选择（两次点击）
    - 向左为交叉选择(Crossing Selection)，交叉选择只要和实体有交叉就能选中
    - 向右为(Window Selection)窗口选择，窗口选择则必须将实体完整包围才能选中
    - 选择时（或者无命令执行时）点击一个点默认就是框选，向左移动时交叉选择，向右移动为窗口选择
- 多边形选择(Polygon Selection)：由多个点指定多边形进行判定
    - 多边形交叉(Polygon Crossing Selection)，也翻译为**圈交(CP)**，有交叉即选中
    - 多边形窗口(Polygon WindowSelection)，也翻译为**圈围(WP)**，全部包围才能选中
    - 一般通过选择的分支进入
- 套索(Lasso Selection)选择：按下鼠标左键，不放开移动鼠标，形成一个平滑的套索，
    - 套索交叉选择(Lasso Crossing Selection)：交叉即选中
    - 套索窗口选择(Lasso Window Selection)：全部包围选中
    - 按下鼠标左键不放开就进入套索选择，逻辑上需要和框选进行区分，一般通过按下左键移动一定距离（比如XY只有有一边达到100个像素）后如果还没有放开左键就进入套索选择直到左键放开套索完成，小范围内已经放开左键则进入框选，拖动预览等待第二点，如果一直按着左键鼠标也不动那么就延后再判定
    - 和框选一个入口通过按住鼠标左键不放进入，一般没有其他进入途径
- 栏选(Fence Selection): 类似line命令流程，画出一系列线段，于这些线段相交的实体被选中
    - 通过分支进入
- 全选(All)：不知道是通过命令(selectall)实现的功能，还是需要选择框架原生支持(比如通过某个选择分支)，待定

选择交互实现研究：
- InputContext内部集成SelectionTask，作为一个被动状态机，由InputContext来管理其状态
- InputContext内部维护瞬态数据，比如框选的框现在有多大，光标应该是什么形状之类，由Renderer获取之后进行渲染
- InputContext本身实现OnUpdate，其中调用SlectionTask的onUpdate（根据状态判断要调用哪个task，后续方便扩展），在runCommandLoop中先调InputContext::onUpdate，再调用命令的onUpdate
- InputContext的SelectionTask如果在框选状态下，那么输入会先给SelectionTask，SelectionTask运行结束后才会将输入给到命令
- Shift减选或者反选，需要判断实体状态与Shift是否按下，InputContext中可能需要维护Shift状态，由InputHandler来管理，再handleLeftMouseClick来进行判断。
- Shift减选，或者选择集已经有实体了，那么再选就是加选

选择交互研究：
- 非命令中，按下左键进入选择交互，估计是通过放开左键时移动距离的判断进入套索还是框选流程
- 框选预览流程中，等待下一个点，同时可以接受关键字进入其他选择分支：
    - 如果不在命令运行中，则显示提示：`指定对角点或 [栏选(F)/圈围(WP)/圈交(CP)]:`（交互层提示）
    - 如果在命令中则显示命令的提示（可通过判断命令提示是否为空来做，不为空则覆盖选择原生提示）
    - 就算命令提示中没有选择分支F/WP/CP依然可以进入，因为选择实体的流程一般都没有其他关键字，所以一般不会和命令关键字冲突
    - 每一个与选择框相交或者完全包含其中的实体被临时高亮
    - 点击下一个点则框选结束，返回选中的实体
    - Enter/Space提示：`窗口说明无效。`（交互层提示）
    - Esc取消框选过程
    - 但如果在命令中进入框选流程并不会直接取消命令流程，而是取消框选过程后，无实体被加入选择集，推测并没有返回kCanceled，而是返回kEntitySelection
        - 比如move命令，点击指定第一个点后进入框选，然后Esc会取消框选，并提示`找到 0 个`（应该是命令层的提示）
- 栏选F：
    - F分支进入，进入栏选后框选流程结束，提示`指定下一个栏选点或 [放弃(U)]:`（交互层提示），栏选第一个点就是框选的第一个点
    - 类似于line命令，每次新点击一个点，产生一条固定下来的线段，无论是当前预览线段还是已经固定的线段都绘制为虚线
    - 每一个和这些固定线段以及预览线段相交的实体都被临时高亮
    - U表示Undo，会撤销上一个点，如果刚进入分支或者多次撤销后，只有第一个点的情况，U无任何效果，第一个点不会被撤销，并继续执行
    - Enter/Space结束栏选，返回选中实体
    - Esc取消栏选，不选中任何实体，结束栏选过程，同时也结束整个选择过程
    - 如果在命令中Esc，还会直接结束命令过程（当然如果命令有多层，不一定是直接结束，而是返回kCanceled由命令流程来处理）
- 圈围WP/圈交CP:
    - 也就是窗口多边形选择，圈交也就是多边形交叉选择是虚线，绿色覆盖，圈围也就是多边形窗口选择是实线，蓝色覆盖，其他流程一模一样
    - 框选的第一点作为多边形第一点，第二点还未选择时，预览是一条线（实线或者虚线，可以和多边形绘制统一起来），预览后续点时，则正常绘制多边形的线段和内部颜色
    - 提示为`指定直线的端点或 [放弃(U)]:`
    - U撤销上一个点，只有一个点时同样无任何效果
    - 内部实体同样被临时高亮
    - Enter/Space结束多边形选择，返回选中实体
    - Esc取消多边形选择，不选中任何实体，结束多边形选择过程，同时也结束整个选择过程
    - 命令中Esc同样结束整个命令，选择过程返回kCanceled由命令流程处理
- 套索选择：
    - 套索选择和框选来自同一个入口，即命令中选择过程中按下鼠标左键或者非命令执行中按下左键
    - 如果鼠标左键按下不抬起，在屏幕上持续移动，则会持续绘制套索，直到左键抬起
    - 那么如何区分框选和套索选择呢，如果按下左键不移动一定时间后放开也是进入框选过程，判定和时间无任何关系
    - 而是通过预览点（当前光标位置）移动一段距离后，还没有抬起就进入套索选择，如果已经放开则进入框选
        - 比如定为上下左右只要哪一边移动超过10个像素还没有抬起那么就进入套索选择，直接鼠标抬起则套索选择结束
    - 套索选择会记录一系列的点，但并不是每一帧都需要记录（每一帧都记录会导致记录一堆重复点，有严重性能问题且没有意义）
        - 更好的选择是同时记录上一点，然后判定当前点是否与上一点移动超过10个像素，如果超过10个像素，那么才将点加入列表，否则就不加入列表
        - 最后一点会和第一点闭合形成闭合多边形，由这个多边形来判定是否选中了实体
    - 套索也有窗口选择和交叉选择，默认情况下只根据第二点来判定，第二点如果在第一点左侧，就是交叉选择，否则就是窗口选择
    - 然后非常重要的是，套索选择过程中按Enter/Space会切换选择模式：
        - 比如进入时通过第二点判断得知当前是窗口(W)套索选择，那么会输出依次提示`窗交(C) 套索  按空格键以循环选项`
        - 按下空格会在`C W F`之间循环，按下一次则切换一次，同时输出`窗口(W) 套索  按空格键以循环选项` `栏选(F) 套索  按空格键以循环选项`
        - 按照`C -> W -> F -> C -> W ...`的顺序循环，切换时才会输出对应的提示，移动过程中每次切换都输出一次提示
        - 切换后预览、光标标记同时切换
    - 按着鼠标左键进行套索选择过程中Esc会结束套索选择过程，无任何实体被加入选择集，不会取消命令流程
    - 套索中的栏选是按住鼠标进行的，和前面分支进入的栏选虽然原理相同，但是代码应当不是同一个
- 单选：
    - 不会在无命令执行的过程中进入，只会在命令中选择中由命令调用相关接口来触发
    - 单选时，光标显示为仅选择框的样式，按下左键进行选择，按下时有实体在选择框内，将该实体加入选择集，结束选择过程
    - 如果没有实体在选择框内，则进入框选或者套索选择流程（判定规则上面已经说过），点击的坐标作为框选的第一点
    - 后续流程就是框选或者套索流程，也可以进入WP/CP/F分支或者通过空格切换套索选择模式
    - 单选同时也支持F/WP/CP分支，不过提示由命令层提供，可能是`选择对象:`
    - Esc取消选择过程，返回kCanceled由命令流程处理
    - Enter/Space结束选择，返回kEnterInput给命令流程处理
- Shift减选：
    - 和以上逻辑正交，完全不冲突
    - 如果在单选点击时Shift处于按下状态，那么单选变为减选，点击实体时会将该实体从选择集中移除
    - 如果是框选、套索、多边形选择，在第一点输入时（以上逻辑第一点都是框选的第一点，入口都一样）如果shift处于按下状态，那么最终就是减选，选择结束实体会被从选择集中移除
    - 不按住shift则是加选，实体被加入选择集
    - 减选加选会显示相关标记，但如果同时在进行窗口、交叉选择，则窗口、交叉标记将覆盖加选减选标记

特殊流程：
- 单选时同样可以进入分支，`F/WP/CP`，此时从第一个点开始指定栏选、圈围、圈交，暂时无预览
- 提示分别为：
    - F分支：`指定第一个栏选点或拾取/拖动光标:`
    - WP分支：`指定第一个圈围点或拾取/拖动光标:`
    - CP分支：`指定第一个圈交点或拾取/拖动光标:`
- 此时可以按下拖动进入对应的栏选、圈围、圈交套索流程

实现研究：
- 将所有选择按照上述流程合并实现为一个SelectionTask，由InputContext来管理其状态
- 入口可定制，比如命令中从单选进入，非实体位置点击第一点后可进入框选、套索，命令外则从点击第一点开始直接进入框选或者套索不会进入单选
- 后续WP/CP/F则只能通过分支进入
- 当前实体、选择集都未实现，相关位置注释应该有的操作即可，后续可能会实现当前工作选择集、先选选择集等，选择任务可能也需要绑定特定选择集，这些暂时都不需要实现
- 不同层级分离：
    - 交互层：只需要收集选择相关矩形、多边形、点
    - 检索层：拿着这些数据去空间索引里面找实体的以及找了存在哪儿
    - 结果层；处理去重、高亮、命令层的命令行输出
    - 目前只需要实现交互层：另外两层后续会逐步补充


TODO:
- Autocad的交互，选择框架支持的操作(move命令选择实体交互为例)：
    - `需要点或窗口(W)/上一个(L)/窗交(C)/框(BOX)/全部(ALL)/栏选(F)/圈围(WP)/圈交(CP)/编组(G)/添加(A)/删除(R)/多个(M)/前一个(P)/放弃(U)/自动(AU)/单个(SI)/子对象(SU)/对象(O)`
    - 全部实现远比我想象的要复杂，不过最重要的几个选择交互现在都已经有了，还没有实现的都需要实体、选择集等功能完善之后才好实现
    - 窗口(W)/窗交(C): 非常简单，框选做一个限制即可，固定为窗口或者窗交
    - 上一个(L)：选择最新创建的图形，
    - 全部(ALL)：比较简单，需要数据库、选择集支持
    - 编组(G)：需要实现为对对象编组(group命令)的功能之后才能实现
    - 添加(A)：手动指定添加模式
    - 删除(R)：手动指定删除模式
    - 多个(M)：单选模式中允许选择多个对象，Enter才返回
    - 前一个(P)：选择上一次选择的对象，需要选择集支持
    - 放弃(U)：放弃上一次加入选择集的实体
    - 单个(SI)：单选模式，和多选相互切换
    - 子对象(SU)：允许选择子对象，需要实体、子对象相关功能支持
    - 对象(O)：不知道什么作用
    - 命令select：支持所有选择交互的选择命令
- 支持定制允许的选择类型，比如无命令时仅允许Box/WP/CP/F和套索，而select命令则支持所有。
- erase命令可能需要定制选择任务，其流程和正常流程不同，单选点击没有实体的位置进入栏选流程。

## 命令注册、命令别名、命令补全

为了提高命令执行框架的可用性与用户友好程度，需要实现别名与补全功能，系统性管理别名、命令全称，实现命令查找、实时根据输入查找命令列表提供补全建议列表。

命令别名：
- 常见命令参考别名列表：

| 分类 | 命令全称 (Command) | 别名 (Alias) | 功能描述 (Description) |
| :--- | :--- | :--- | :--- |
| 绘图 | LINE | L | 直线 |
| | CIRCLE | C | 圆 |
| | PLINE | PL | 多段线 |
| | RECTANG | REC | 矩形 |
| | ARC | A | 圆弧 |
| | ELLIPSE | EL | 椭圆 |
| | HATCH | H | 图案填充 |
| | BLOCK | B | 创建块 |
| | INSERT | I | 插入块 |
| | POINT | PO | 点 |
| | SPLINE | SPL | 样条曲线 |
| 编辑 | ERASE | E | 删除 |
| | MOVE | M | 移动 |
| | COPY | CO / CP | 复制 |
| | ROTATE | RO | 旋转 |
| | SCALE | SC | 比例缩放 |
| | TRIM | TR | 修剪 |
| | EXTEND | EX | 延伸 |
| | OFFSET | O | 偏移 |
| | MIRROR | MI | 镜像 |
| | FILLET | F | 倒圆角 |
| | CHAMFER | CHA | 倒角 |
| | EXPLODE | X | 分解 |
| | STRETCH | S | 拉伸 |
| | ARRAY | AR | 阵列 |
| | JOIN | J | 合并线段 |
| 标注 | DIMLINEAR | DLI | 线性标注 |
| | DIMALIGNED | DAL | 对齐标注 |
| | DIMANGULAR | DAN | 角度标注 |
| | DIMRADIUS | DRA | 半径标注 |
| | DIMDIAMETER | DDI | 直径标注 |
| | DIMSTYLE | D | 标注样式管理器 |
| | TEXT | DT | 单行文字 |
| | MTEXT | T / MT | 多行文字 |
| 管理 | ZOOM | Z | 视图缩放 |
| | PAN | P | 平移 |
| | REGEN | RE | 重新生成图形 |
| | LAYER | LA | 图层管理器​​ |
| | PURGE | PU | 清理垃圾 |
| | UNITS | UN | 图形单位 |
| | WBLOCK | W | 写块 (导出) |
| | OSNAP | OS | 对象捕捉设置 |
| | OPTIONS | OP | 系统选项 |
| | PROPERTIES | PR / MO| 特性/属性面板 |
| | MATCHPROP | MA | 特性匹配/格式刷 |
| | UNDO | U | 撤销 |
| | QUIT | EXIT | 退出程序 |

命令注册：
- `CommandManager`负责。
- 命令注册时指定命令全名，别名，全大写，然后绑定到一个命令类。
- 定义`std::function<std::unique_ptr<Command>()>`为命令对象的生成器对象，定义个函数模板传递命令类的类型进来使用其构造函数作为生成器即可，然后就可以将全名、对应生成器对象存在一张表中了。
- 别名到全名的映射需要再存一张表，就可以通过别名找到全名，再找到生成器进而构造命令对象了。
- 还需要实现输入字符串，获取补全列表的功能。
- 将所有命令、别名信息构造如下结构并排序后存入列表，方便查询以及生成补全信息：
```C++
// 补全项：用于UI渲染与排序
struct CommandItem {
    std::string key;        // 匹配键(别名或全称)
    std::string fullName;   // 命令全名
    bool isAlias;           // 权重标签

    // 优先级：别名 > 字母顺序，越小排越前，优先级越高
    bool operator<(const CommandItem& other) const {
        if (isAlias != other.isAlias) return isAlias;
        return key < other.key;
    }
    // 举例: {"L", "LINE", true} 排于 {"LINE", "LINE", false}前，输入L时同时匹配。
};
```
- 所有命令注册完成后生成并排序所有命令，别名和全称会分成两项，有两个别名那就有三项。
- 获取补全列表时，顺序查找，只要输入字符是`key`的前缀，就加入返回结果，从前往后顺序建立列表，返回结果是自然排好序的。

补全、候选框、候选匹配的逻辑：
- 当没有活跃交互时（没有任何命令或者选择或者夹点编辑等活动任务时），输入字符，将解析为命令：
    - 为其实时生成一个补全列表
    - 弹出候选命令列表框，按照优先级从高到低，显示在命令输入框上方。
    - 按下Tab键将键入的内容补全为第一项，再按下Tab切换为第二项，以此类推，Enter/Space提交执行。
- 按照前缀匹配：
    - 优先级规则：匹配某个别名前缀 > 匹配命令全名的前缀 > (后续可能会实现的模糊匹配)
    - 比如输入`L`，优先匹配`L LA`，然后才是`LINE LAYER`。
- 候选框：
    - 在输入框中输入命令（也就是焦点位于输入框）时弹出候选框，显示在命令输入框上方。
    - 可以使用菜单实现（但感觉不是很好，菜单需要焦点，而输入框也需要交焦点，可以用ImGui表格实现），
    - 鼠标移动到上方点击则会直接执行对应命令。
    - 没有任何候选项时、或者命令栏什么字符也没有就不会显示。
    - 根据候选列表来显示，匹配别名前缀的显示在最上方，比如输入`L`，列表项有`L (LINE)` `LA (LAYER)` `LINE` `LAYER` ...，输入`LAY`则显示`LAYER ...`
    - 命令输入框焦点丢失则候选框消失。
- 交互细节：
    - 每一次通过键盘输入或删除字符则重建候选列表，所以这个候选列表是需要跨帧保存下来的。
    - 输入了什么字符也需要重新保存一份，比如输入`l`，tab补全为`LINE`，这时候并再用`LINE`去重新生成一个候选列表，而是一直都是`l`的候选列表。
    - 以输入`l`为例：
        - 那么Renderer将会记录下当前输入字符是`l`，转为大写`L`后去获取并保存候选列表。
        - 假设候选列表有4项，候选框会渲染在输入栏上方，内容为`L (LINE)` `LA (LAYER)` `LINE` `LAYER`，第一项自动高亮（表示现在就回车默认执行的命令）。
        - 按下一次Tab补全为`LINE`，也就是第一项的全名，补全后全转成大写，但是此时补全列表还是`L`的补全列表，候选框第一项处于选中状态。
        - 有一项被选中之后就不需要任何项高亮了，高亮和选中是两种状态。
        - 再按下一次Tab补全为第二项`LAYER`，后面第三次第四次是`LINE LAYER`，第五次则从第一项`LINE`开始循环。
        - 如果此时删除字符、焦点切换到其他地方又切换回来，那么会重新根据当前命令框的所有字符去建立候选列表。
        - 每一次补全后，自动在输入框中填入补全而来的字符，比如`l`补全到`LINE`，INE会被选中，光标位于尾端，那么一次Backspace就可以删除补全的字符，以方便输入后续字符。
        - Enter/Space会提交执行，就算没有手动Tab补全为完整命令，输入`l/lI/Lin`都是直接执行`LINE`，因为它是首选列表第一项（假设列表中`LINE`都是第一项）。
Todo：
- 模糊匹配。

命令执行历史，up/down滚动导航选择：
- 关联到文档，不会持久化记录，一个会话维护一个命令执行历史列表，每一项就只有一个命令字符串。
- 命令执行历史不会有重复记录，同一条命令只会有一条，比如连续执行两次line命令，命令历史末尾只会只会有一条line命令。
- 只记录已知命令，无效命令不会被记录，新命令执行后和历史命令列表中某一个相同时需要将历史中的移除，将新一条添加到末尾。
- 没有在任何命令或者交互任务中时，按下up键进入导航模式(如果此时命令历史中没有任何命令，那么则不会进入导航模式)，导航模式下当前命令行已有的输入会被直接删除，将命令输入栏切换为命令历史中的命令，光标位于末尾。
- up/down在命令历史列表中切换。
- 一直up到列表中第一个时，再按up不会变化，保持为第一条。
- 一直down到最后一条之后再down一下，就清空输入栏，退出导航模式。
- 导航过程中命令被编辑，比如添加或者删除一个字符同样退出导航模式。
- 导航模式下不会进行命令补全，不会弹出补全候选窗口。
- 导航模式下Esc退出导航模式，不会将`LINE*Cancel*`这样的消息输出到命令行历史中去。
- 焦点切换相关处理：
    - 在绘图区（更一般地来说，焦点为空时）up/down，类似于Enter/Space/Esc，会将焦点切换到命令输入框，并切换为对应的命令历史（或者清空输入）。
    - 在命令行历史窗口获得焦点时（此时允许鼠标移出窗口，不会强制剥夺焦点），up/down会同时滚动命令行历史窗口（imgui原生，支持按住连续滚动），并且同时进行命令历史导航（只响应按下，不响应按住），并且不剥夺与设置焦点。
    - 焦点位于输入框中时，正常切换历史命令。
- 完整实现了所有功能，与Autocad在行为上会略微有点差别：
    - autocad在命令历史上编辑后也不会退出命令历史导航模式，但补全会开始介入，重新up/down切换其他历史命令后，补全又重新消失。
    - 没有必要跟随这样做，因为补全实现都是有差别的，autocad补全框是允许up/down介入，而这里只允许Tab参与。
    - 并且同时修复了补全的bug。

## 空间索引实现研究

## 选择集功能研究

## 属性栏

Todo:
- 定义选择服务(SelectionService)，在选择时将实体交给属性栏进行属性显示
- 而在命令中进行选择交互时，选择服务则不将实体传递给属性栏，属性栏不显示实体实时属性

## 捕捉与追踪功能研究

## 脚本交互研究

TODO:
- CAD程序使用命令进行交互，那么天生就适合将命令写成脚本顺序执行
- 最简单的实现手段就是将脚本按顺序一个个解释为命令，发送给交互模块即可完成
- 这是实现自动化测试必不可少的一环
- 也可以实现或集成程序化的脚本引擎，实现程序化的脚本，类似Autocad的Lisp引擎

## 动态输入研究

动态输入：
- 开关：F12
- 目前没有实现任何与动态输入相关的内容
- 待研究


## 功能实现顺序

AI生成的功能实现参考。

### 第一阶段：核心架构、数据逻辑分离

- 1、几何数据模型（Database/DOM）：
    - 建立一个 Document 类，持有所有的几何图元（Line, Circle, Arc...）。
    - 关键点：使用双精度浮点数 double 或高精度定点数处理坐标，千万不要用单精度 float，否则缩放时会出现严重的精度抖动。
- 2、命令系统（Command Pattern）
    - 每一个修改文档的操作都封装成一个 Command 对象。
    - 实现 UndoStack（撤销栈）。这是 CAD 的命脉，后期补救非常困难
- 3、坐标转换引擎（Transformer）：
    - 实现核心函数：WorldToScreen() 和 ScreenToWorld()。
    - 管理 Camera（持有 Pan 偏移和 Zoom 缩放倍率）

### 第二阶段：渲染管线、画布集成

- 1、FBO视口集成
    - 不要直接在窗口绘图。将几何图形渲染到 OpenGL FBO (Framebuffer Object)，然后通过 ImGui::Image() 显示。
    - 这样你可以轻松实现多文档对比、视口缩放而不干扰 ImGui UI。
- 2、即时绘制（Immediate Layers）：
    - 区分“静态数据渲染”（已保存的线段）和“动态层渲染”（鼠标正在拉出的虚线、捕捉提示）。
    - 利用 ImGui::GetWindowDrawList() 快速实现动态反馈。

### 第三阶段：交互框架、状态机

- 这是 CAD 程序最复杂的部分：非模态交互。
- 1、输入解析器（Tokenizer）：
    - 解析 InputText 传来的字符串（如 L 10,20）
- 2、交互状态机：
    - CAD 处于不同状态：Idle（选择模式）、DrawingLine（等待第一个点）、DrawingLine_Step2（等待第二个点）
    - 输入优先级：当用户按下回车或鼠标点击时，逻辑先发给 ActiveTool。如果 ActiveTool 为空，再发给全局命令解析器。
- 3、坐标捕捉系统（Snapping - 简化版）：
    - 先实现 网格捕捉（Grid Snap）：将鼠标坐标 round 到最近的网格点。这是后续实现复杂对象捕捉（端点、中点）的基础。

### 第四阶段：依次完善功能

- 按照顺序，依次实现

|顺序|	功能模块|	目标|
|:-:|:-:|:-
|1|	视图操作|	鼠标中键平移、滚轮缩放 FBO 内的内容（确保坐标转换无误）。|
|2|	基础解析器|	能够识别 LINE 命令并进入“取点状态”。|
|3|	点输入系统|	支持两种方式取点：鼠标点击、命令行输入 x,y 坐标。|
|4|	图元生成|	执行命令后，真正将 Line 对象加入 Document 并渲染出来。|
|5|	撤销|/重做	确保每画一根线都能被撤销，保证数据一致性。|
|6|	多文档切换|	实现 Tab 或多窗口，验证 Document 类的解耦是否彻底。|
|7|	对象捕捉|	实现端点、交点捕捉，这是从“画图板”进化为“CAD”的标志。|


特别建议：
- 不要在回调里写业务逻辑：InputText 的回调只负责“接收输入”和“清理缓冲区”。真正的绘图逻辑应该由一个 CommandDispatcher 在主循环的 Update 阶段处理。
- 处理 Esc 键：CAD 用户极其依赖 Esc。确保你的状态机能随时响应 Esc 并重置当前所有未完成的命令，同时通过 g_needs_clear 清空输入框。
- 坐标系习惯：OpenGL 默认是Y轴向上，而窗口坐标（ImGui）是Y轴向下。在底层逻辑中，建议统一使用数学笛卡尔坐标系（Y轴向上），仅在最后的渲染阶段进行反转。

## 透明命令研究

- ZOOM、PAN、GRID 这种不修改几何数据、只改变观察状态的操作，是不应该中断当前正在进行的绘图（如 LINE）或修改（如 MOVE）命令的

三个核心层级的逻辑隔离：
- 为了支持透明操作，主循环输入处理逻辑应该按以下优先级排序：
- 1、系统级/视图级（透明层）
    - 鼠标滚轮/中键：直接修改 Camera 对象的 Zoom 和 Offset
    - 快捷键（如 F7 切换网格）：直接修改全局渲染标志
    - 特点：执行完后，不重置当前命令状态。
- 2、 命令解析层（指令层）
    - InputText 回车：解析字符串。如果是 ZOOM 或 PAN 文本命令，修改 Camera；如果是 LINE，启动状态机。
- 3、当前活动命令层（业务层）
    - 如果当前有一个 ActiveCommand（比如正在画线，已点下第一点），它会“监听”鼠标左键点击。
    - 特点：直到命令完成或按 Esc 才会释放

状态机（State Machine）的设计建议：
- 命令内部采用状态驱动：
- Idle 状态：没有命令执行。命令行输入 LINE -> 实例化 LineTool
- Active 状态：
    - LineTool 内部维护一个子状态（Step 0: 等待起点, Step 1: 等待终点）。
    - 透明性体现：当用户在 Step 1 时滚动鼠标中键，Camera 变了，但 LineTool 依然停留在 Step 1。

CAD框架关键技术点：
- 坐标系解耦：严格区分三种坐标，写好转换函数
    - World Space (世界坐标)：你的几何数据存储位(doudle,doule)
    - Canvas Space (画布/FBO 坐标)：相对于 FBO 左上角的像素坐标。
    - Screen Space (窗口坐标)：ImGui 的全局坐标（用于处理 InputText 和菜单）。
    - world --------Camera------> Canvas -------ImGuiWindowPos-------> Screen
- 渲染分层：
    - 静态层：渲染 Document 中的所有线段。
    - 橡皮筋层 (Rubber Banding)：如果正在画线，实时根据鼠标当前位置画出一根“虚线”到起点。这一步直接在 ImGui::Image() 之后的 ImDrawList 上画，不要去修改 FBO。

